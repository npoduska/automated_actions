name: daily-report

on:
   schedule:
     - cron: '0 10 * * *'
   workflow_dispatch:

jobs:
  my-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "hi there world!"
