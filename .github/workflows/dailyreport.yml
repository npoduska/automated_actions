name: daily-report

on:
   schedule:
     - cron: '0 10  * * 1-6' # Run every day at 10am, monday-saturday
   workflow_dispatch:

jobs:
  my-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "hi there world!"
